<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Edit <%= player.name %></title>
</head>
<body>
    <a href="/players" id="link">Back</a>
    <h1>Edit <%= player.name %></h1>

    <div class="container mt-5">
        <form class="row g-4" action="/players/<%= player._id %>?_method=PUT" method="POST">
            <div class="col-md-6">
                <label for="name" class="form-label">Name:</label>
                <span class="text-danger">*</span>
                <input type="text" id="name" name="name" class="form-control" value="<%= player.name %>" required>
            </div>

            <div class="col-md-6">
                <label for="image" class="form-label">Image:</label>
                <input type="text" id="image" name="image" class="form-control" value="<%= player.image %>">
            </div>
            
            <div class="col-md-6">
                <label for="nationality" class="form-label">Nationality:</label>
                <span class="text-danger">*</span>
                <input type="text" id="nationality" name="nationality" class="form-control" value="<%= player.nationality %>" required>
            </div>

            <div class="col-md-6">
                <label for="team" class="form-label">Team:</label>
                <span class="text-danger">*</span>
                <input type="text" id="team" name="team" class="form-control" value="<%= player.team %>" required>
            </div>

            <div class="col-md-6">
                <label for="rating" class="form-label">Rating:</label>
                <span class="text-danger">*</span>
                <input type="number" id="rating" name="rating" class="form-control" value="<%= player.rating %>" required min="1" max="99">
            </div>

            <div class="col-md-6">
                <label for="isCurrentlyPlaying" class="form-label">Is Currently Playing:</label>
                <div class="form-check">
                    <input type="checkbox" id="isCurrentlyPlaying" name="isCurrentlyPlaying" class="form-check-input"
                    <% if (player.isCurrentlyPlaying) { %>
                        checked
                    <% } %>
                    >
                    <label class="form-check-label" for="isCurrentlyPlaying">Yes</label>
                </div>
            </div>
            
            <div class="col-12">
                <button type="submit" class="btn btn-primary">Save Changes</button>
            </div>
        </form>
    </div>
</body>
</html>